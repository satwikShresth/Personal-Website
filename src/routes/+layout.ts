export const prerender = true;
export const trailingSlash = "always";

// SSR is enabled here to allow MDX components to be rendered during build
// The final output is still static HTML files - no server needed at runtime
